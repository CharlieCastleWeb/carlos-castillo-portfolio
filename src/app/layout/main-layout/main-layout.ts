import { Component } from '@angular/core';
import { Header } from '../header/header';

@Component({
  selector: 'ccp-main-layout',
  imports: [Header],
  templateUrl: './main-layout.html',
})
export class MainLayout {}
